This is a simple Backbone extension library that gives alternative mechanism to navigate between application views.

Conceptually it based on ViewNavigator from Apache Flex framework so the views can be pushed or popped from the stack. It is especially useful in mobile application scenarios.